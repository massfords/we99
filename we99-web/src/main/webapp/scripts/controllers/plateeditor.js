'use strict';

/**
 * @ngdoc function
 * @name we99App.controller:PlateeditorCtrl
 * @description
 * # PlateeditorCtrl
 * Controller of the we99App
 */
angular.module('we99App')
  .controller('PlateEditorCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
