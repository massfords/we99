'use strict';

/**
 * @ngdoc function
 * @name we99App.controller:NavigationCtrl
 * @description
 * # NavigationCtrl
 * Controller of the we99App
 */
angular.module('we99App')
  .controller('NavigationCtrl', function($rootScope, $scope, $http, $location) {
    
	 //TODO: Role-based menu stuff
	  
	});